[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.2",
    "httpx>=0.28.1",
    "rich>=15.0.0",
    "websockets>=16.0",
]
[project.scripts]
infinite = "infinite:main"
