# Skill: Deploy App (Meta-Orchestration Optimized)

## Objective
Your goal as the DevOps Master (@devops) is to ensure the Antigravity Agentic Environment is correctly configured and ready for rule-driven orchestration.

## Instructions
1. **HAL Check**: Verify that the execution environment is permitted by reading `agents.yaml` and running `scripts/hal-check.ps1` natively via terminal.
2. **Setup Detection**: This is a Meta-Orchestration project. Check that `.agents/rules/`, `.agents/workflows/`, and `.gemini/agents/` directories are present and contain valid Markdown.
3. **Internal Verification**: If requested, perform a `npx skills list` or equivalent to verify that any required external skills are registered.
4. **Local Readiness**: Ensure that `docs/track/TODO.md` is initialized and reachable.
5. **Report**: Output the status of the "AI Agent Factory" to the user and confirm the triad is active!
