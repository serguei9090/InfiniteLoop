# Test Environment Documentation

## Overview

This folder contains test-specific configurations and documentation for the IMMUTABLE CORE Orchestrator system.

## Architecture Context

- **Core Backend**: Located in `core/` (IMMUTABLE, READ-ONLY)
- **New Features**: Place new router modules in `Apps/*/*.py`
- **Frontend**: Located in `ui/` (READ-ONLY)
- **Sandbox**: General workspace at `workspace/`

## Project Structure

- core/               # Backend (FastAPI) - IMMUTABLE
  - main.py          # Entry point (auto-loads routers)
- Apps/              # YOUR WORKSPACE for new features
  - feature_name/router.py  # Auto-included API routes
- ui/                # Frontend (React) - READ-ONLY
  - src/
- test/              # Test documentation & configs
- workspace/         # General sandbox
- README.md

## Setup Instructions

### Prerequisites

Python 3.9+ on Windows 11 (AMD64)

### Start Backend Server

```powershell
cd core
python main.py
```

Server will start at http://localhost:8000

### Auto-Loading Routers

Any .py file with a Router class inside Apps/*/ will be automatically loaded by core/main.py.

## Usage Examples

### Create New Feature

1. Navigate to Apps/ folder
2. Create new feature directory
3. Create router.py inside it
4. Define your API routes in router.py
5. Restart backend server to auto-load routes

### Test Documentation Location

This folder (test/) is designated for:
- Testing documentation
- Feature validation notes
- Environment-specific configurations

## API Reference

All routers in Apps/*/*.py are automatically registered with the main FastAPI application.

### Health Check

curl http://localhost:8000/docs

## Best Practices

1. DO NOT modify: core/, ui/ (READ-ONLY)
2. DO create new features: In Apps/*/
3. Use sandbox for experimentation: workspace/
4. Keep test docs here: test/

## IMMUTABLE CORE PRINCIPLES

- Core services remain unchanged
- New capabilities expand through modular addition
- Zero-downtime deployments via router auto-loading
- Frontend remains stable and read-only

---
Generated by IMMUTABLE CORE Orchestrator
System Status: Operational