[project]
name = "immutable-core-workspace"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "litellm>=1.83.0",
]

[tool.uv]
managed = true

[tool.uv.workspace]
members = ["core", "cli"]
